#ifndef _RENDERNODE_H_
#define _RENDERNODE_H_
/*
#include <list>

class RenderVisitor;

class RenderNode
{
public:
  virtual void Visit(RenderVisitor * visitor) = 0;
};

class RenderGroup : public RenderNode
{
  std::list<RenderNode *> mChildren;
public:
  virtual void Visit(RenderVisitor * visitor);
  void AddChild(RenderNode * child);
  void RemoveChild(RenderNode * child);
};
*/
#endif
