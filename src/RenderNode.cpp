#include "RenderNode.h"
/*
void RenderGroup::Visit(RenderVisitor * visitor)
{
  for (auto it = mChildren.begin(); it != mChildren.end(); ++it)
  {
    RenderNode * child = *it;
    child->Visit(visitor);
  }
}

void RenderGroup::Draw(RenderVisitor * visitor)
{
  for (auto it = mChildren.begin(); it != mChildren.end(); ++it)
  {
    RenderNode * child = *it;
    child->Draw(visitor);
  }
}

void RenderGroup::AddChild(RenderNode * child)
{
  mChildren.push_back(child);
}

void RenderGroup::RemoveChild( RenderNode * child )
{
  mChildren.remove(child); // just linear is fine
}
*/
